# Keyaos — Worker Secrets (local development)
# Copy to .dev.vars and fill in your values.
# Production secrets: set via `wrangler secret put <NAME>`

# ─── Core (required) ─────────────────────────────────────
ADMIN_TOKEN=changeme
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# ─── Platform (optional — enables multi-tenancy) ─────────
# PLATFORM_OWNER_ID=user_xxxxx
# CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
# CLERK_SECRET_KEY=sk_test_xxxxx
# STRIPE_SECRET_KEY=sk_test_xxxxx
# STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# ─── Model Catalog Sync ──────────────────────────────────
# When CATALOG_URL is set, the worker syncs its model_pricing table
# from the remote catalog every minute (recommended for self-hosted).
# When unset, the worker runs local syncAllModels using system keys below.
CATALOG_URL=https://keyaos.com/api/catalog

# ─── Provider System Keys (optional — local sync mode only) ──
# Only needed when CATALOG_URL is NOT set.
# The sync service fetches live models from provider APIs.
# DEEPSEEK_KEY=sk-xxxxx
# OPENAI_KEY=sk-proj-xxxxx
# GEMINI_KEY=AIzaxxxxx
# ANTHROPIC_KEY=sk-ant-api03-xxxxx
